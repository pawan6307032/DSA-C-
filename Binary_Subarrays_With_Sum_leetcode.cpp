#include <iostream>
#include <vector>
using namespace std;

int problem(vector<int> a, int sum)
{
    
}

int main()
{
    vector<int> v = {0,0,0,0,0};
    int sum = 0;

    int ans = problem(v, sum);
    cout << ans;
    return 0;
}