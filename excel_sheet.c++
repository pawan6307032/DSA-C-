#include <iostream>
#include <string>
#include <math.h>
using namespace std;

int value(string str)
{
    int ans = 0, p = 0;
    for (int i = str.length() - 1; i >= 0; i--)
    {
        char ch = str[i];
    }
    return ans;
}

int main()
{
    string str = "AA";
    // cout << "Enter :";
    // cin >> str;
    cout << value(str);

    return 0;
}